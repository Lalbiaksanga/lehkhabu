# Lehkhabu backend-ai — Python dependencies
# Zero Redis. PostgreSQL handles everything.

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.30

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10  # sync driver for Alembic migrations
alembic==1.14.1
pgvector==0.3.6

# Settings
pydantic-settings==2.7.1

# AI / Embeddings
google-generativeai==0.8.4
tiktoken==0.8.0

# Document parsing
docling==2.16.0

# HTTP client (for fetching book files)
httpx==0.28.1

# Background tasks (PostgreSQL as broker, no Redis)
celery[sqlalchemy]==5.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
